// Footer.test.js
import React from "react";
import { render } from "@testing-library/react";
import Footer from "./Footer";

test("renders Footer component", () => {
  render(<Footer />);
});

// Adicione mais testes conforme necessário para garantir que todos os componentes internos estão renderizando corretamente.
